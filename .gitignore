__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
build/
dist/
output.wem
.coverage
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/
.understand-anything/
.workflow/
/crates/target/
/examples/rust/target/
js/node_modules/
*.so
*.dylib
*.pyd
*.whl
native-wheel/

# wasm-pack --out-dir output (web target -> js/pkg, nodejs target -> js/pkg-node).
# Build output like target/ and the wheels above: `make wasm-build` before the
# Node parity test (`make wasm-test`) or the browser demo.
js/pkg/
js/pkg-node/

# Unverified extraction/derived data (the analysis tool dumps, corpus candidates).
# Nothing under corpus/ is a trusted reference asset until verified and moved.
corpus/

# macOS Finder metadata (generated noise, never an intentional artifact).
.DS_Store

# the round paired-corpus tooling outputs (scratch templates; not versioned assets).
rs_corpus_*.xml
.claude/settings.local.json

.claude
.pi
.pi-lens.json