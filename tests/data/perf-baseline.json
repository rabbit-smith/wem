{
  "schema": "wwise-wem.perf-baseline.v1",
  "fixture": "tests/fixtures/input.wav",
  "profile": "6ch/44100Hz/2013.2",
  "measurement": "release build of crates/wem-core; wwise-wem --time 'encode' stage median over 7 runs (warm)",
  "runs": 7,
  "median_encode_ms": 127.15,
  "cap_ms": 150.0,
  "regression_factor": 1.25,
  "notes": "Recorded on the development machine (Apple Silicon, release build). The 150ms cap is the normative budget for any runner; the regression factor guards relative drift against this recorded baseline. Re-record via `python3 scripts/perf_check.py --record` only with an explicit decision."
}
