[package]
name = "wem-container"
description = "WEM encoder RIFF/WEM container models and codecs (no resource loading)."
version.workspace = true
edition.workspace = true

[dependencies]

[dev-dependencies]
serde_json = { workspace = true }
sha2 = { workspace = true }
